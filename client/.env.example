# Copy to .env and fill in. Never commit the filled-in file.

# Where the game server lives. In development this is the local server; in
# production, set it in your host's environment (e.g. Vercel project settings).
VITE_SERVER_URL=http://localhost:4000
